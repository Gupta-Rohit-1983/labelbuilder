package com.rohit.labelbuilder.model.style;

/** Horizontal text alignment within a text element's bounds (lbl-format.md §4.2). */
public enum TextAlign {
    LEFT,
    CENTER,
    RIGHT,
    JUSTIFY
}
