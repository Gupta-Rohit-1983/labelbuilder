package com.rohit.labelbuilder.automation;

/**
 * Placeholder marker for the lb-automation module skeleton (Phase 2b).
 * Replaced by real code in its owning phase; exists so the module has a
 * compilable class and a runnable test from day one.
 */
public final class AutomationModule {

    /** Module name as declared in the Maven reactor. */
    public static final String MODULE_NAME = "lb-automation";

    private AutomationModule() {}
}
